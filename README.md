### **Часть 1: Введение и подготовка (5 минут)**

**Этап 1: Приветствие и представление темы**

Добрый день! Сегодня мы с вами погрузимся в один из самых популярных и востребованных инструментов современной фронтенд-разработки — библиотеку React.js.

Наша цель — не просто изучить сухие термины, а понять, как эти концепции работают вживую, в реальном приложении. Поэтому сегодняшняя лекция будет полностью практической. Мы будем писать код, сразу видеть результат и постепенно, шаг за шагом, добавлять в наше приложение новые возможности.

**Этап 2: Анонс финального результата**

К концу этого полуторачасового занятия мы вместе создадим полноценное интерактивное веб-приложение — вашу личную библиотеку или «Список книг для чтения».

Это приложение будет уметь:
*   Показывать список ваших книг.
*   Позволять добавлять в него новые книги.
*   Отмечать книги как прочитанные.
*   Сохранять все ваши данные, даже если вы перезагрузите страницу.
*   Показывать отдельную страницу с подробной информацией по каждой книге.
*   Автоматически подгружать обложки и описания из интернета.
*   И выглядеть всё это будет стильно и аккуратно.

Мы будем как строители: сначала заложим фундамент, потом возведём стены, а в конце — наведём красоту и подключим коммуникации.

**Этап 3: Краткий обзор ключевых концепций**

Чтобы построить такое приложение, нам потребуется освоить несколько ключевых идей React:
*   **Компоненты и JSX:** Это кирпичики, из которых состоит любое React-приложение. Мы будем думать не о целой странице, а о независимых, переиспользуемых блоках.
*   **Состояние (State):** Это то, что делает приложение «живым» и реагирующим на ваши действия. Данные, которые могут меняться.
*   **Эффекты (Effects):** Это механизм для выполнения «побочных» действий: например, сохранения данных или загрузки информации из интернета.
*   **Маршрутизация (Routing):** Позволяет создать в нашем одностраничном приложении иллюзию нескольких страниц, как в классическом сайте.
*   **Работа с API:** Научимся общаться с внешним миром, чтобы получать свежие данные.
*   **UI-библиотеки:** Воспользуемся готовыми красивыми компонентами, чтобы не изобретать велосипед.

Не переживайте, если какие-то термины пока не до конца понятны. Мы разберём каждый из них на конкретном примере, и к концу лекции они станут вашими хорошими знакомыми.

Итак, приступим к коду.

Отлично! Переходим от введения к самой интересной части — созданию нашего первого React-компонента.

***

### **Часть 2: Создание фундамента приложения (20 минут)**

#### **Этап 1: Первый компонент и знакомство с JSX**

Сердце любого React-приложения — это **компоненты**. Представьте, что вы собираете конструктор: у вас есть готовые блоки (кнопка, шапка, карточка товара), и вы из них собираете интерфейс. Компонент в React — это как раз такой самостоятельный, переиспользуемый блок.

Давайте создадим наш самый главный компонент — `App`. Это будет корневой компонент, в который мы будем вкладывать все остальные.

**Шаг 1: Создаем файл `App.js`**
Обычно каждый компонент располагается в своем собственном файле с расширением `.js` или `.jsx`.

**Шаг 2: Импортируем React**
В самом начале файла нам нужно импортировать главную библиотеку. Раньше это было обязательным требованием, но в самых свежих версиях это уже не всегда нужно. Однако, для ясности и совместимости, давайте сделаем это.

```jsx
// App.js
import React from 'react';
```

**Шаг 3: Пишем наш первый функциональный компонент**
Современный React в основном использует функциональные компоненты. Это просто JavaScript-функция, которая возвращает то, что мы хотим увидеть на экране.

```jsx
// App.js
import React from 'react';

// Объявляем функцию с именем App
function App() {
  // Эта функция должна что-то вернуть
  return (
    // То, что возвращается, выглядит как HTML, но это JSX!
    <div className="app">
      <h1>Мой список книг</h1>
      <p>Добро пожаловать в вашу личную библиотеку!</p>
    </div>
  );
}
```

**Шаг 4: Разбираемся с JSX**
Вы видите, что оператор `return` возвращает что-то, что очень похоже на HTML. Этот синтаксис называется **JSX** — он позволяет нам писать HTML-подобный код прямо внутри JavaScript.

Есть несколько ключевых отличий JSX от HTML, и одно из самых важных — это использование `className` вместо `class`. Слово `class` в JavaScript зарезервировано для создания классов, поэтому в JSX для задания CSS-классов используется атрибут `className`.

*   В HTML: `<div class="app">`
*   В JSX: `<div className="app">`

**Шаг 5: Экспортируем компонент**
Чтобы другие части нашего приложения могли использовать этот компонент, его необходимо экспортировать.

```jsx
// App.js
import React from 'react';

function App() {
  return (
    <div className="app">
      <h1>Мой список книг</h1>
      <p>Добро пожаловать в вашу личную библиотеку!</p>
    </div>
  );
}

// Экспортируем компонент по умолчанию
export default App;
```

Вот и всё! Мы создали наш первый React-компонент. Пока что он статичный и всегда показывает один и тот же текст. Но это тот самый фундамент, на котором мы будем строить всё остальное.

Давайте теперь "оживим" его, научив хранить данные.

Отлично! Наш компонент уже есть, но он статичен. Пришло время наполнить его данными, которые могут меняться. Переходим к одной из самых важных концепций React — состоянию.

***

#### **Этап 2: "Оживление" данных с помощью состояния (useState)**

Сейчас наш список книг "зашит" прямо в верстку. Что, если мы захотим добавить книгу или отметить ее как прочитанную? Нам нужен способ хранить данные, которые могут изменяться, и чтобы React автоматически перерисовывал интерфейс при их изменении. Для этого и существует **состояние (state)**.

**Шаг 1: Проблема статичных данных**
Попробовать изменить переменную прямо в коде — это не сработает. React не узнает об изменении и не перерисует компонент. Нам нужен специальный механизм.

**Шаг 2: Импортируем хук `useState`**
`useState` — это так называемый **"хук" (hook)**. Хуки — это специальные функции в React, которые позволяют "подключаться" к возможностям React, таким как состояние, из функциональных компонентов.

```jsx
// App.js
import React, { useState } from 'react'; // Импортируем useState
```

**Шаг 3: Объявление состояния**
Мы используем `useState` прямо внутри нашего компонента, чтобы создать "ячейку" для хранения данных.

```jsx
// App.js
import React, { useState } from 'react';

function App() {
  // Вызываем useState и передаем ему начальное значение
  const [books, setBooks] = useState([]); // Начинаем с пустого массива

  return (
    <div className="app">
      <h1>Мой список книг</h1>
      <p>Добро пожаловать в вашу личную библиотеку!</p>
    </div>
  );
}

export default App;
```

Давайте разберем эту строку `const [books, setBooks] = useState([]);`:

*   `useState([])` создает состояние. В скобках мы указываем начальное значение — в нашем случае пустой массив `[]`.
*   Возвращает он массив из двух элементов:
    1.  **Первое значение (`books`)** — это текущее состояние, актуальные данные. В первый раз это будет тот самый пустой массив `[]`.
    2.  **Второе значение (`setBooks`)** — это функция-сеттер. Единственный способ изменить состояние `books` — это вызвать `setBooks()` и передать туда новое значение.

Синтаксис квадратных скобок `[books, setBooks]` — это деструктуризация массива. Она просто позволяет нам сразу получить эти два значения в отдельные переменные.

**Шаг 4: Инициализация состояния данными**
Давайте для начала заполним наш массив книгами, чтобы было что показывать. Каждая книга будет объектом с полями: уникальный `id`, `title`, `author` и флажок `isRead` (прочитана или нет).

```jsx
// App.js
import React, { useState } from 'react';

function App() {
  // Инициализируем состояние массивом книг
  const [books, setBooks] = useState([
    { id: 1, title: 'Дюна', author: 'Фрэнк Герберт', isRead: false },
    { id: 2, title: '1984', author: 'Джордж Оруэлл', isRead: true },
    { id: 3, title: 'Мастер и Маргарита', author: 'Михаил Булгаков', isRead: false },
  ]);

  return (
    <div className="app">
      <h1>Мой список книг</h1>
      <p>Добро пожаловать в вашу личную библиотеку!</p>
    </div>
  );
}

export default App;
```

**Шаг 5: Используем состояние в JSX**
Давайте просто выведем количество книг в нашем состоянии, чтобы убедиться, что все работает.

```jsx
// App.js
// ... импорт и useState

function App() {
  const [books, setBooks] = useState([
    // ... массив книг
  ]);

  return (
    <div className="app">
      <h1>Мой список книг</h1>
      {/* Обратите внимание на фигурные скобки — это "ворота" из JSX в JavaScript */}
      <p>В вашей библиотеке: <strong>{books.length}</strong> книг(и)</p>
    </div>
  );
}

export default App;
```

Теперь в интерфейсе мы увидим сообщение "В вашей библиотеке: 3 книг(и)". Это доказывает, что наше состояние `books` работает, и мы можем использовать его данные в разметке.

Мы создали состояние! Теперь эти данные не статичны, они управляются React. Следующий шаг — отобразить их в виде красивого списка.

Идеально! Теперь у нас есть данные, и мы даже вывели их количество. Но пользователю не очень интересно видеть просто цифру "3". Давайте отобразим наши книги в виде красивого и наглядного списка.

***

#### **Этап 3: Динамический рендеринг списка**

Нам нужно преобразовать наш массив объектов `books` в массив JSX-элементов, которые React сможет отобразить на странице. Для этого в JavaScript есть идеальный метод — `map()`.

**Шаг 1: Использование метода `map()` для преобразования данных**

Метод `map()` проходит по каждому элементу исходного массива, позволяет нам его преобразовать и возвращает *новый* массив с результатами этих преобразований. Мы преобразуем массив книг в массив JSX-тегов `<li>`.

```jsx
// App.js
import React, { useState } from 'react';

function App() {
  const [books, setBooks] = useState([
    { id: 1, title: 'Дюна', author: 'Фрэнк Герберт', isRead: false },
    { id: 2, title: '1984', author: 'Джордж Оруэлл', isRead: true },
    { id: 3, title: 'Мастер и Маргарита', author: 'Михаил Булгаков', isRead: false },
  ]);

  return (
    <div className="app">
      <h1>Мой список книг</h1>
      <p>В вашей библиотеке: <strong>{books.length}</strong> книг(и)</p>

      {/* Создаем ненумерованный список */}
      <ul>
        {
          // Используем map для преобразования books в массив <li>
          books.map(book => {
            // Для каждого объекта "book" возвращаем JSX
            return (
              <li>
                <strong>{book.title}</strong> - {book.author}
              </li>
            );
          })
        }
      </ul>
    </div>
  );
}

export default App;
```

**Шаг 2: Важность и назначение пропа `key`**

Если вы откроете консоль браузера в этом моменте, вы увидите предупреждение: *"Warning: Each child in a list should have a unique 'key' prop."* (Каждый дочерний элемент в списке должен иметь уникальный проп `key`).

Почему это важно? Когда React рисует список, ему нужно однозначно идентифицировать каждый элемент, особенно когда этот список может меняться (добавление, удаление, сортировка). `key` помогает React понять, какой именно элемент изменился, и перерисовать только его, а не весь список, что делает обновление интерфейса очень быстрым.

**Шаг 3: Добавляем ключ (key) каждому элементу списка**

В качестве `key` мы должны использовать уникальную и стабильную строку. Идеально для этого подходит `id` нашей книги.

```jsx
// App.js
// ... (импорт и состояние)

function App() {
  // ... (состояние books)

  return (
    <div className="app">
      <h1>Мой список книг</h1>
      <p>В вашей библиотеке: <strong>{books.length}</strong> книг(и)</p>

      <ul>
        {books.map(book => {
          return (
            // Добавляем атрибут key с уникальным id книги
            <li key={book.id}>
              <strong>{book.title}</strong> - {book.author}
            </li>
          );
        })}
      </ul>
    </div>
  );
}

export default App;
```

Теперь предупреждение в консоли исчезнет, и наш список будет работать оптимально. Мы уже видим наши книги на странице! Но они пока что просто статичный текст. Давайте добавим немного визуальной информации и, что самое главное, интерактивности.

Прекрасно! У нас есть красивый список книг. Но наш список для чтения должен быть интерактивным. Давайте добавим возможность менять статус книги — отмечать её как прочитанную или непрочитанную. Это идеальный повод продемонстрировать, как работает обновление состояния.

***

#### **Этап 4: Добавление интерактивности - изменение состояния**

Сейчас у каждой книги в данных есть поле `isRead`. Давайте будем отображать это визуально и дадим пользователю кнопку, чтобы переключать этот статус.

**Шаг 1: Визуальное отображение статуса книги**

Давайте добавим небольшую иконку-индикатор перед названием каждой книги. Например, 📖 для "к прочтению" и ✅ для "прочитано".

```jsx
// App.js
// ... (импорт и состояние)

function App() {
  // ... (состояние books)

  return (
    <div className="app">
      <h1>Мой список книг</h1>
      <p>В вашей библиотеке: <strong>{books.length}</strong> книг(и)</p>

      <ul>
        {books.map(book => {
          return (
            <li key={book.id}>
              {/* Добавляем условный рендеринг иконки */}
              <span>{book.isRead ? '✅ ' : '📖 '}</span>
              <strong>{book.title}</strong> - {book.author}
            </li>
          );
        })}
      </ul>
    </div>
  );
}

export default App;
```

Здесь мы используем тернарный оператор `book.isRead ? '✅ ' : '📖 '`. Это читается так: "Если `book.isRead` истинно, вернуть '✅ ', иначе вернуть '📖 '".

**Шаг 2: Добавление кнопки для изменения статуса**

Теперь добавим саму кнопку, которая будет запускать процесс изменения.

```jsx
// App.js
// ... (импорт и состояние)

function App() {
  // ... (состояние books)

  return (
    <div className="app">
      <h1>Мой список книг</h1>
      <p>В вашей библиотеке: <strong>{books.length}</strong> книг(и)</p>

      <ul>
        {books.map(book => {
          return (
            <li key={book.id}>
              <span>{book.isRead ? '✅ ' : '📖 '}</span>
              <strong>{book.title}</strong> - {book.author}
              {/* Добавляем кнопку с обработчиком onClick */}
              <button>
                {book.isRead ? 'Отметить как непрочитанную' : 'Отметить как прочитанную'}
              </button>
            </li>
          );
        })}
      </ul>
    </div>
  );
}

export default App;
```

Текст на кнопке тоже меняется динамически в зависимости от текущего статуса.

**Шаг 3: Логика обновления состояния в обработчике `onClick`**

Теперь самое главное — заставить кнопку работать. Мы не можем просто написать `book.isRead = !book.isRead`. **Состояние в React неизменяемо (immutable). Мы никогда не меняем его напрямую.** Вместо этого мы создаем *новую* версию состояния и передаем её функции-сеттеру.

Правильный алгоритм:
1.  Создать *новый* массив книг на основе старого.
2.  В этом новом массиве найти нужную книгу и создать *новый* объект для неё с измененным свойством `isRead`.
3.  Передать этот новый массив в `setBooks`.

Реализуем это с помощью методов `map()` и spread-оператора (`...`).

```jsx
// App.js
// ... (импорт и состояние)

function App() {
  // ... (состояние books)

  return (
    <div className="app">
      <h1>Мой список книг</h1>
      <p>В вашей библиотеке: <strong>{books.length}</strong> книг(и)</p>

      <ul>
        {books.map(book => {
          return (
            <li key={book.id}>
              <span>{book.isRead ? '✅ ' : '📖 '}</span>
              <strong>{book.title}</strong> - {book.author}
              <button
                onClick={() => {
                  // 1. Создаем обновленный массив книг
                  const updatedBooks = books.map(b => {
                    // 2. Если это текущая книга, возвращаем её КОПИЮ с измененным isRead
                    if (b.id === book.id) {
                      return { ...b, isRead: !b.isRead }; // Spread-оператор создает копию объекта
                    }
                    // 3. Для всех остальных книг возвращаем их без изменений
                    return b;
                  });
                  // 4. Обновляем состояние, передавая новый массив
                  setBooks(updatedBooks);
                }}
              >
                {book.isRead ? 'Не прочитана' : 'Прочитана'}
              </button>
            </li>
          );
        })}
      </ul>
    </div>
  );
}

export default App;
```

**Разберем ключевую строку: `return { ...b, isRead: !b.isRead };`**

*   `...b` (spread-оператор) — создает поверхностную копию всех свойств объекта книги `b`.
*   `, isRead: !b.isRead` — после копирования мы говорим: "перь свойство `isRead` перезапиши новым значением, противоположным старому (`!b.isRead`)".

Это классический подход в React: мы не меняем старые данные, мы создаем новые на их основе.

Теперь нажимайте на кнопки! Вы увидите, как меняются иконки и текст на кнопках. Наше приложение стало по-настоящему интерактивным. Мы освоили одну из самых фундаментальных операций в React — обновление состояния.

Блестяще! Мы создали интерактивный список. Но пока что мы работаем только с заранее заданными данными. Пришло время дать пользователю возможность самостоятельно пополнять свою библиотеку. Для этого нам понадобится форма. Это познакомит нас с ещё одной важнейшей концепцией — контролируемыми компонентами.

***

### **Часть 3: Работа с пользовательским вводом и данными (20 минут)**

#### **Этап 5: Создание формы для добавления новых книг**

Форма будет состоять из двух полей ввода и кнопки. Нам нужно не только отобразить эти элементы, но и "привязать" их к состоянию React.

**Шаг 1: Создание формы и полей ввода**

Давайте добавим форму прямо в наш компонент `App`, под списком книг.

```jsx
// App.js
// ... (импорт и состояние)

function App() {
  // ... (состояние books)

  return (
    <div className="app">
      <h1>Мой список книг</h1>
      <p>В вашей библиотеке: <strong>{books.length}</strong> книг(и)</p>

      {/* Список книг (остается без изменений) */}
      <ul>
        {books.map(book => (
          <li key={book.id}>
            <span>{book.isRead ? '✅ ' : '📖 '}</span>
            <strong>{book.title}</strong> - {book.author}
            <button onClick={() => {
              const updatedBooks = books.map(b =>
                b.id === book.id ? { ...b, isRead: !b.isRead } : b
              );
              setBooks(updatedBooks);
            }}>
              {book.isRead ? 'Не прочитана' : 'Прочитана'}
            </button>
          </li>
        ))}
      </ul>

      {/* НАЧАЛО: Новая форма для добавления книги */}
      <form>
        <h2>Добавить новую книгу</h2>
        <div>
          <label htmlFor="title">Название:</label>
          <input
            id="title"
            type="text"
          />
        </div>
        <div>
          <label htmlFor="author">Автор:</label>
          <input
            id="author"
            type="text"
          />
        </div>
        <button type="submit">Добавить в список</button>
      </form>
      {/* КОНЕЦ: Новая форма */}
    </div>
  );
}

export default App;
```

Обратите внимание на `htmlFor` в теге `<label>`. Это JSX-эквивалент атрибута `for` в HTML.

**Шаг 2: Создание отдельных состояний для полей ввода**

Нам нужно где-то хранить то, что пользователь вводит в поля "Название" и "Автор". Для каждого поля создадим свое отдельное состояние.

```jsx
// App.js
import React, { useState } from 'react';

function App() {
  const [books, setBooks] = useState([
    { id: 1, title: 'Дюна', author: 'Фрэнк Герберт', isRead: false },
    { id: 2, title: '1984', author: 'Джордж Оруэлл', isRead: true },
    { id: 3, title: 'Мастер и Маргарита', author: 'Михаил Булгаков', isRead: false },
  ]);

  // Создаем состояния для полей формы
  const [title, setTitle] = useState(''); // Начинаем с пустой строки
  const [author, setAuthor] = useState(''); // Начинаем с пустой строки

  // ... (возвращаемый JSX)
}

export default App;
```

**Шаг 3: Создание "контролируемых компонентов"**

Это ключевой момент. Мы превращаем наши inputs в **контролируемые компоненты**. Это означает:
1.  Значение input привязано к состоянию React (атрибут `value`).
2.  Каждое нажатие клавиши в input вызывает обновление состояния (через `onChange`).
3.  React становится единственным источником истины для значения поля.

Свяжем наши поля с состоянием:

```jsx
// App.js
// ... (импорт и состояния books, title, author)

function App() {
  // ... (состояния)

  return (
    <div className="app">
      {/* ... (заголовок и список книг) ... */}

      <form>
        <h2>Добавить новую книгу</h2>
        <div>
          <label htmlFor="title">Название:</label>
          <input
            id="title"
            type="text"
            value={title} // Значение привязано к состоянию title
            onChange={(e) => setTitle(e.target.value)} // Обновляем состояние при каждом изменении
          />
        </div>
        <div>
          <label htmlFor="author">Автор:</label>
          <input
            id="author"
            type="text"
            value={author} // Значение привязано к состоянию author
            onChange={(e) => setAuthor(e.target.value)} // Обновляем состояние при каждом изменении
          />
        </div>
        <button type="submit">Добавить в список</button>
      </form>
    </div>
  );
}

export default App;
```

Теперь, если вы попробуете ввести текст в любое из полей, вы ничего не увидите. Почему? Потому что значение поля жестко привязано к состоянию, которое изначально пустое. Но при этом, если вы добавите `console.log(title)` внутрь функции `App`, вы увидите, что при каждом нажатии клавиши состояние обновляется! Нам нужно завершить цикл: обновление состояния заставляет компонент перерисовываться, и input получает новое значение из состояния. Теперь поля будут работать как ожидалось.

**Шаг 4: Обработка отправки формы**

Теперь нужно сделать так, чтобы при нажатии на кнопку "Добавить в список" книга добавлялась в массив `books`. Для этого обработаем событие `onSubmit` формы.

```jsx
// App.js
// ... (импорт и состояния books, title, author)

function App() {
  // ... (состояния)

  // Функция-обработчик отправки формы
  const addBook = (e) => {
    e.preventDefault(); // ОБЯЗАТЕЛЬНО! Предотвращаем перезагрузку страницы

    // Простая проверка, что поля не пустые
    if (title.trim() === '' || author.trim() === '') {
      alert('Пожалуйста, заполните все поля');
      return;
    }

    // Создаем новый объект книги
    const newBook = {
      id: Date.now(), // Простой способ получить уникальный ID (время в миллисекундах)
      title: title,
      author: author,
      isRead: false, // Новая книга по умолчанию не прочитана
    };

    // Обновляем состояние books, добавляя новую книгу
    setBooks([...books, newBook]); // Spread-оператор создает новый массив: старые книги + новая

    // Шаг 5: "Сброс" формы - очищаем поля ввода
    setTitle('');
    setAuthor('');
  };

  return (
    <div className="app">
      {/* ... (заголовок и список книг) ... */}

      {/* Подключаем функцию addBook к событию отправки формы */}
      <form onSubmit={addBook}>
        <h2>Добавить новую книгу</h2>
        <div>
          <label htmlFor="title">Название:</label>
          <input
            id="title"
            type="text"
            value={title}
            onChange={(e) => setTitle(e.target.value)}
          />
        </div>
        <div>
          <label htmlFor="author">Автор:</label>
          <input
            id="author"
            type="text"
            value={author}
            onChange={(e) => setAuthor(e.target.value)}
          />
        </div>
        <button type="submit">Добавить в список</button>
      </form>
    </div>
  );
}

export default App;
```

Теперь вы можете добавлять новые книги в список! Обратите внимание, как после добавления поля формы автоматически очищаются — это происходит потому, что мы сбросили состояния `title` и `author` на пустые строки, а поскольку поля контролируемые, они мгновенно отреагировали на это изменение.

Мы освоили работу с формами и контролируемыми компонентами. Теперь наше приложение умеет не только отображать, но и получать данные от пользователя.

Потрясающе! Наше приложение уже очень функциональное. Однако есть проблема: если мы обновим страницу, все добавленные нами книги исчезнут, и мы вернемся к исходному списку. Давайте это исправим, научив наше приложение сохранять данные прямо в браузере пользователя. Для этого мы познакомимся с мощным инструментом — хуком `useEffect`.

***

#### **Этап 6: Сохранение данных в LocalStorage с помощью useEffect**

**Шаг 1: Проблема: потеря данных при обновлении страницы**

Состояние React живет в оперативной памяти. Перезагрузка страницы очищает её, и наше приложение запускается с начальным состоянием, которое мы задали в `useState([])`. Нам нужно синхронизировать наше состояние с чем-то более постоянным.

**Шаг 2: Импорт хука `useEffect`**

`useEffect` — это второй по важности хук после `useState`. Он позволяет выполнять "побочные эффекты" в функциональных компонентах. Побочный эффект — это любое действие, которое выходит за рамки возврата JSX: работа с API, таймеры, подписки на события и, что нам нужно, работа с LocalStorage.

```jsx
// App.js
import React, { useState, useEffect } from 'react'; // Импортируем useEffect
```

**Шаг 3: Назначение `useEffect`**

`useEffect` принимает два аргумента:
1.  Функция, которая содержит код эффекта.
2.  Массив зависимостей (dependencies). Он определяет, при изменении каких переменых эффект должен запускаться заново.

**Шаг 4: Эффект для сохранения данных при каждом изменении `books`**

Мы хотим, чтобы каждый раз, когда массив `books` обновляется (добавили книгу, изменили статус), мы сохраняли его в LocalStorage.

```jsx
// App.js
import React, { useState, useEffect } from 'react';

function App() {
  const [books, setBooks] = useState([]); // Теперь начинаем с ПУСТОГО массива
  const [title, setTitle] = useState('');
  const [author, setAuthor] = useState('');

  // Эффект для СОХРАНЕНИЯ книг в LocalStorage
  useEffect(() => {
    // Преобразуем массив books в строку JSON и сохраняем под ключом 'my-reading-list'
    localStorage.setItem('my-reading-list', JSON.stringify(books));
    // Этот эффект будет запускаться после каждого рендера, при котором изменилось значение `books`
  }, [books]); // <-- Массив зависимостей: [books]

  // ... (функция addBook и JSX остаются без изменений)
}

export default App;
```

**Разберем массив зависимостей `[books]`:**
*   Эффект запустится после первого рендера компонента (монтирования).
*   Затем он будет запускаться *только* после тех рендеров, в ходе которых значение `books` изменилось.
*   Если бы массив был пустым `[]`, эффект запустился бы только один раз при монтировании.
*   Если бы массива не было вообще, эффект запускался бы после *каждого* рендера.

**Шаг 5: Эффект для загрузки данных при первом рендере компонента**

Теперь нам нужно при запуске приложения проверить, нет ли уже сохраненных книг в LocalStorage, и если есть — загрузить их в наше начальное состояние.

```jsx
// App.js
import React, { useState, useEffect } from 'react';

function App() {
  const [books, setBooks] = useState([]);
  const [title, setTitle] = useState('');
  const [author, setAuthor] = useState('');

  // Эффект для ЗАГРУЗКИ книг из LocalStorage при старте приложения
  useEffect(() => {
    // Пытаемся получить данные по ключу 'my-reading-list'
    const savedBooks = JSON.parse(localStorage.getItem('my-reading-list'));
    // Если данные найдены и это массив...
    if (savedBooks && Array.isArray(savedBooks)) {
      setBooks(savedBooks); // ...устанавливаем их в качестве начального состояния
    }
  }, []); // <-- ПУСТОЙ массив зависимостей. Этот эффект сработает только ОДИН раз при монтировании.

  // Эффект для сохранения (остается без изменений)
  useEffect(() => {
    localStorage.setItem('my-reading-list', JSON.stringify(books));
  }, [books]);

  // ... (функция addBook и JSX)
}

export default App;
```

**Важный момент:** Мы изменили начальное значение состояния `books` на пустой массив `[]`. Почему? Потому что теперь мы не хотим, чтобы при каждом запуске показывались одни и те же тестовые данные. Мы хотим сначала проверить LocalStorage, и если там есть данные, использовать их.

**Как это работает теперь:**
1.  Компонент `App` монтируется.
2.  Запускается эффект с `[]` (загрузка). Он проверяет LocalStorage и, если находит там книги, вызывает `setBooks(savedBooks)`.
3.  Вызов `setBooks` вызывает повторный рендер компонента с новыми данными.
4.  При этом рендере массив `books` уже не пустой, поэтому срабатывает эффект с `[books]` (сохранение) и обновляет LocalStorage актуальными данными.
5.  В дальнейшем эффект сохранения будет срабатывать при любом изменении `books`.

Теперь вы можете добавлять книги, менять их статус, обновлять страницу — и ваши данные никуда не денутся! Мы реализовали полноценное сохранение состояния приложения между сессиями.

Идеально! Наше приложение теперь сохраняет данные. Но посмотрите на наш компонент `App` — он становится большим и начинает совмещать слишком много задач: отображение списка, управление формой, логику сохранения. Пришло время навести порядок и разбить его на smaller, более управляемые части. Это основа масштабируемых React-приложений.

***

### **Часть 4: Структурирование приложения и маршрутизация (15 минут)**

#### **Этап 7: Рефакторинг - разделение на компоненты**

Мы вынесем список книг и форму добавления в их собственные, отдельные компоненты. Это сделает код чище, проще для понимания и позволит переиспользовать эти компоненты в будущем.

**Шаг 1: Вынесение списка книг в компонент `BookList`**

Создадим новый компонент прямо в этом же файле (для простоты примера; в большом проекте их лучше выносить в отдельные файлы).

```jsx
// App.js
// ... (импорты и состояние App)

// Главный компонент App
function App() {
  const [books, setBooks] = useState([]);
  const [title, setTitle] = useState('');
  const [author, setAuthor] = useState('');

  // ... (useEffect и функция addBook)

  return (
    <div className="app">
      <h1>Мой список книг</h1>
      <p>В вашей библиотеке: <strong>{books.length}</strong> книг(и)</p>

      {/* Вместо прямого вывода списка, используем компонент BookList */}
      {/* Передаем ему нужные данные и функции через пропсы */}
      <BookList books={books} setBooks={setBooks} />

      {/* Форма пока остается здесь */}
      <form onSubmit={addBook}>
        {/* ... (тело формы) ... */}
      </form>
    </div>
  );
}

// Создаем новый компонент BookList ВНЕ функции App
// Он принимает props (свойства) - объект с полями `books` и `setBooks`
function BookList({ books, setBooks }) {
  // Этот компонент возвращает ТОЛЬКО JSX для списка
  return (
    <ul>
      {books.map(book => (
        <li key={book.id}>
          <span>{book.isRead ? '✅ ' : '📖 '}</span>
          <strong>{book.title}</strong> - {book.author}
          <button onClick={() => {
            const updatedBooks = books.map(b =>
              b.id === book.id ? { ...b, isRead: !b.isRead } : b
            );
            setBooks(updatedBooks);
          }}>
            {book.isRead ? 'Не прочитана' : 'Прочитана'}
          </button>
        </li>
      ))}
    </ul>
  );
}

export default App;
```

**Что изменилось:**
*   Логика отрисовки списка теперь находится в отдельной функции `BookList`.
*   Компонент `App` передает ей два **пропса** (props): текущий массив `books` и функцию `setBooks` для его обновления.
*   Компонент `BookList` получает эти пропсы как аргумент функции. Мы используем деструктуризацию `{ books, setBooks }`, чтобы сразу получить их как переменные.

**Шаг 2: Вынесение формы добавления книги в компонент `AddBookForm`**

Проделаем то же самое с формой.

```jsx
// App.js
// ... (импорты и состояние App)

function App() {
  const [books, setBooks] = useState([]);
  const [title, setTitle] = useState('');
  const [author, setAuthor] = useState('');

  // ... (useEffect)

  const addBook = (e) => {
    e.preventDefault();
    if (title.trim() && author.trim()) {
      const newBook = {
        id: Date.now(),
        title,
        author,
        isRead: false,
      };
      setBooks([...books, newBook]);
      setTitle('');
      setAuthor('');
    }
  };

  return (
    <div className="app">
      <h1>Мой список книг</h1>
      <p>В вашей библиотеке: <strong>{books.length}</strong> книг(и)</p>

      <BookList books={books} setBooks={setBooks} />

      {/* Заменяем форму на компонент AddBookForm */}
      {/* Передаем ему все необходимые состояния и функции */}
      <AddBookForm
        title={title}
        setTitle={setTitle}
        author={author}
        setAuthor={setAuthor}
        addBook={addBook}
      />
    </div>
  );
}

// Компонент BookList (остается без изменений)
function BookList({ books, setBooks }) {
  // ... (тот же JSX)
}

// Создаем новый компонент для формы
function AddBookForm({ title, setTitle, author, setAuthor, addBook }) {
  return (
    <form onSubmit={addBook}>
      <h2>Добавить новую книгу</h2>
      <div>
        <label htmlFor="title">Название:</label>
        <input
          id="title"
          type="text"
          value={title}
          onChange={(e) => setTitle(e.target.value)}
        />
      </div>
      <div>
        <label htmlFor="author">Автор:</label>
        <input
          id="author"
          type="text"
          value={author}
          onChange={(e) => setAuthor(e.target.value)}
        />
      </div>
      <button type="submit">Добавить в список</button>
    </form>
  );
}

export default App;
```

Теперь наш компонент `App` стал гораздо чище! Он выполняет роль "менеджера состояния": хранит данные и главные функции, а затем делегирует отображение более мелким, специализированным компонентам. Это классическая и мощная архитектура в React.

Теперь, когда у нас есть четкое разделение, мы можем легко добавить навигацию между этими компонентами, сделав их разными "страницами".

Отлично! Мы навели порядок в структуре. Теперь наше приложение выглядит как набор переиспользуемых блоков. Но все эти блоки отображаются на одной странице. Давайте превратим наше приложение в многостраничное, где список книг и форма добавления будут на разных "страницах", а также добавим детальную страницу для каждой книги. Для этого мы воспользуемся библиотекой `react-router-dom`.

***

#### **Этап 8: Внедрение маршрутизации (React Router)**

Маршрутизация позволяет нам сопоставить разные URL-адреса в нашем приложении с разными компонентами. Несмотря на то, что это SPA (Single Page Application), у пользователя будет полная иллюзия работы с несколькими страницами.

**Шаг 1: Установка и импорт `react-router-dom`**

Предположим, что библиотека уже добавлена в проект. Теперь нам нужно импортировать необходимые компоненты.

```jsx
// App.js
import React, { useState, useEffect } from 'react';
// Импортируем компоненты для маршрутизации
import { BrowserRouter as Router, Routes, Route, Link } from 'react-router-dom';
```

*   **`Router`** (`BrowserRouter`): Этот компонент оборачивает всё наше приложение и предоставляет функционал маршрутизации для всех вложенных компонентов.
*   **`Routes`**: Контейнер, внутри которого мы определяем все наши маршруты (`Route`). Он отвечает за то, чтобы отображался только один, наиболее подходящий маршрут.
*   **`Route`**: Определяет правило. Говорит: "когда путь в адресной строке такой-то, покажи такой-то компонент".
*   **`Link`**: Это аналог тега `<a>` (ссылки) в HTML, но он не перезагружает страницу, а только меняет URL в браузере, что приводит к плавной смене компонентов внутри нашего SPA.

**Шаг 2: Оборачивание приложения в `Router`**

Первым делом нужно сделать маршрутизатор доступным для всего приложения.

```jsx
// App.js
function App() {
  // ... (все состояния и эффекты остаются здесь)

  return (
    // Оборачиваем всё в Router
    <Router>
      <div className="app">
        {/* Навигация и Routes будут внутри */}
      </div>
    </Router>
  );
}
```

**Шаг 3: Создание навигационного меню с `Link`**

Добавим постоянную шапку с ссылками, которая будет видна на всех "страницах".

```jsx
// App.js
function App() {
  // ... (состояния)

  return (
    <Router>
      <div className="app">
        {/* Навигационное меню */}
        <nav>
          {/* Link работает как <a href>, но для SPA */}
          <Link to="/">
            <h1>Мой список книг</h1>
          </Link>
          <Link to="/add">Добавить книгу</Link>
        </nav>

        {/* Здесь будут меняться компоненты в зависимости от URL */}
      </div>
    </Router>
  );
}
```

**Шаг 4: Настройка маршрутов `Routes` и `Route`**

Теперь определим, какие компоненты должны показываться по каким адресам. Мы уберем прямое отображение `BookList` и `AddBookForm` из `App` и поместим их в маршруты.

```jsx
// App.js
function App() {
  // ... (состояния и функция addBook)

  return (
    <Router>
      <div className="app">
        <nav>
          <Link to="/"><h1>Мой список книг</h1></Link>
          <Link to="/add">Добавить книгу</Link>
        </nav>

        {/* Контейнер для маршрутов */}
        <Routes>
          {/* Маршрут для главной страницы */}
          {/* Когда путь точно "/", показываем BookList */}
          <Route path="/" element={<BookList books={books} setBooks={setBooks} />} />

          {/* Маршрут для страницы добавления книги */}
          <Route path="/add" element={
            <AddBookForm
              title={title}
              setTitle={setTitle}
              author={author}
              setAuthor={setAuthor}
              addBook={addBook}
            />
          } />
        </Routes>
      </div>
    </Router>
  );
}
```

Теперь проверьте! При запуске приложения вы увидите список книг. Если нажмете "Добавить книгу", URL изменится на `/add`, а список заменится на форму. При этом страница не перезагружается — работает именно так, как мы и хотели.

**Шаг 5: Создание страницы с деталями книги**

Давайте создадим отдельный маршрут для каждой книги, например, `/book/1`, где `1` — это `id` книги.

Для этого нам понадобится два новых компонента и динамический параметр в маршруте. Мы создадим `BookDetail` в следующем сообщении, а пока подготовим для него маршрут и ссылку в списке.

```jsx
// App.js
// ... (импорты)
// Импортируем новый компонент (мы его создадим следующим)
import BookDetail from './BookDetail';

function App() {
  // ... (состояния)

  return (
    <Router>
      <div className="app">
        <nav>...</nav>
        <Routes>
          <Route path="/" element={<BookList books={books} setBooks={setBooks} />} />
          <Route path="/add" element={...} />

          {/* Динамический маршрут для страницы книги. :id - это параметр, который может меняться */}
          <Route path="/book/:id" element={<BookDetail books={books} />} />
        </Routes>
      </div>
    </Router>
  );
}
```

Обратите внимание на путь `path="/book/:id"`. Двоеточие (`:`) означает, что следующая часть URL — это параметр. Его значение (например, `1`, `2`, `123`) мы сможем прочитать внутри компонента `BookDetail`.

**Шаг 5.1: Добавляем ссылки в списке книг**

Модифицируем компонент `BookList`, чтобы название книги вело на её детальную страницу.

```jsx
// Внутри компонента BookList
function BookList({ books, setBooks }) {
  return (
    <div>
      <h2>Все книги</h2>
      <ul>
        {books.map(book => (
          <li key={book.id}>
            <span>{book.isRead ? '✅ ' : '📖 '}</span>
            {/* Заменяем простой текст на Link */}
            <Link to={`/book/${book.id}`}>
              <strong>{book.title}</strong> - {book.author}
            </Link>
            <button onClick={() => { ... }}>
              {book.isRead ? 'Не прочитана' : 'Прочитана'}
            </button>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

Теперь у нас есть навигация между главной страницей, формой добавления и (пока несуществующей) страницей деталей книги. В следующем шаге мы создадим сам компонент `BookDetail`.

Превосходно! Маршрутизация настроена, и ссылки на детальные страницы книг работают. Однако при клике мы попадаем на страницу с ошибкой, потому что компонент `BookDetail` еще не создан. Давайте исправим это и создадим полноценную страницу для просмотра информации о каждой книге.

***

#### **Этап 9: Создание страницы с деталями книги**

Этот компонент будет показывать подробную информацию о выбранной книге. Нам нужно научить его делать две вещи: понимать, какую книгу показывать (получать `id` из URL), и находить объект этой книги в нашем общем массиве.

**Шаг 1: Создаем новый файл для компонента `BookDetail`**

Для лучшей структуры создадим этот компонент в его собственном файле.

```jsx
// BookDetail.js
import React from 'react';
import { useParams, Link } from 'react-router-dom';

function BookDetail({ books }) {
  // Хук useParams позволяет нам получить параметры из URL (в нашем случае :id)
  const { id } = useParams();
  // Находим книгу по ID в массиве books, полученном через пропсы
  // Важно: id из URL — это строка, а в наших данных id — число. Преобразуем в число.
  const book = books.find(b => b.id === Number(id));

  // Если книга не найдена (например, при прямом переходе по неверной ссылке)
  if (!book) {
    return (
      <div>
        <h2>Книга не найдена!</h2>
        <Link to="/">Вернуться на главную</Link>
      </div>
    );
  }

  return (
    <div>
      {/* Кнопка для возврата назад */}
      <Link to="/">← Назад к списку</Link>
      
      {/* Детальная информация о книге */}
      <h1>{book.title}</h1>
      <h2>Автор: {book.author}</h2>
      <p>Статус: <strong>{book.isRead ? 'Прочитана ✅' : 'Еще предстоит прочитать 📖'}</strong></p>
      
      {/* Место для будущей информации из API */}
      <p>Здесь в будущем может быть описание книги, обложка и отзывы!</p>
    </div>
  );
}

export default BookDetail;
```

**Разберем ключевые моменты:**

*   **`useParams()`**: Это хук из `react-router-dom`, который возвращает объект с парами ключ-значение всех параметров из URL. Поскольку наш маршрут был `path="/book/:id"`, у этого объекта будет свойство `id`. Мы используем деструктуризацию, чтобы сразу получить `const { id } = useParams();`.
*   **Поиск книги:** Мы получаем массив `books` через пропсы от родительского компонента `App`. С помощью метода `find()` мы ищем в этом массиве книгу, у которой `id` совпадает с тем, что пришел из URL. Не забываем про преобразование типов (`Number(id)`).
*   **Обработка несуществующей книги:** Всегда важно обрабатывать "крайние случаи". Если пользователь вручную введет несуществующий `id`, мы покажем ему дружелюбное сообщение и ссылку для возврата.
*   **Навигация:** Мы предоставляем пользователю понятный способ вернуться к списку книг.

**Шаг 2: Импортируем `BookDetail` в `App.js`**

Теперь нам нужно сообщить главному компоненту `App` о существовании нового компонента `BookDetail`.

```jsx
// App.js
import React, { useState, useEffect } from 'react';
import { BrowserRouter as Router, Routes, Route, Link } from 'react-router-dom';

// Импортируем компоненты
import BookList from './BookList'; // Предположим, он тоже в отдельном файле
import AddBookForm from './AddBookForm'; // И он тоже
import BookDetail from './BookDetail'; // Импортируем новый компонент

function App() {
  // ... (логика состояния: books, title, author, useEffect, addBook)

  return (
    <Router>
      <div className="app">
        <nav>
          <Link to="/"><h1>Мой список книг</h1></Link>
          <Link to="/add">Добавить книгу</Link>
        </nav>

        <Routes>
          <Route path="/" element={<BookList books={books} setBooks={setBooks} />} />
          <Route path="/add" element={
            <AddBookForm
              title={title}
              setTitle={setTitle}
              author={author}
              setAuthor={setAuthor}
              addBook={addBook}
            />
          } />
          {/* Передаем массив books в BookDetail, чтобы он мог найти нужную книгу */}
          <Route path="/book/:id" element={<BookDetail books={books} />} />
        </Routes>
      </div>
    </Router>
  );
}

export default App;
```

Теперь наше приложение стало по-настоящему многостраничным! Мы можем:
*   Видеть все книги на главной странице (`/`).
*   Переходить на страницу добавления новой книги (`/add`).
*   Кликать на любую книгу в списке и переходить на её уникальную страницу (`/book/1`, `/book/2` и т.д.).
*   Возвращаться назад по истории или с помощью кнопки "Назад".

Мы успешно реализовали маршрутизацию — один из краеугольных камней современных SPA. Теперь наше приложение имеет четкую структуру и навигацию, как у классического веб-сайта, но с скоростью и плавностью одностраничного приложения.

Супер! У нас уже есть полнофункциональное приложение с маршрутизацией. Но давайте сделаем его по-настоящему "умным" и красивым. Сначала научим его получать дополнительную информацию об книгах из интернета, а затем — облачим в стильный современный интерфейс с помощью UI-библиотеки.

***

### **Часть 5: Интеграция с внешним миром и улучшение UI (15 минут)**

#### **Этап 10: Загрузка данных из внешнего API**

Наши книги пока содержат только базовую информацию. Давайте обогатим их, подключившись к Google Books API, чтобы автоматически подгружать обложки и описания.

**Шаг 1: Добавляем состояния для данных API, загрузки и ошибок в `BookDetail`**

При работе с API нам нужно учитывать три состояния: данные загружаются, данные успешно загружены, произошла ошибка.

```jsx
// BookDetail.js
import React, { useState, useEffect } from 'react'; // Импортируем useEffect
import { useParams, Link } from 'react-router-dom';

function BookDetail({ books }) {
  const { id } = useParams();
  const book = books.find(b => b.id === Number(id));

  // Добавляем состояния для работы с API
  const [bookDetails, setBookDetails] = useState(null); // Данные, полученные из API
  const [loading, setLoading] = useState(false); // Флаг загрузки
  const [error, setError] = useState(null); // Сообщение об ошибке

  // ... (проверка на !book остается здесь)

  return (
    <div>
      <Link to="/">← Назад к списку</Link>
      <h1>{book.title}</h1>
      <h2>Автор: {book.author}</h2>
      <p>Статус: <strong>{book.isRead ? 'Прочитана ✅' : 'Еще предстоит прочитать 📖'}</strong></p>
      
      {/* Здесь будем показывать данные из API */}
    </div>
  );
}

export default BookDetail;
```

**Шаг 2: Используем `useEffect` для выполнения запроса к API**

Мы хотим выполнить запрос сразу после того, как компонент смонтировался и мы нашли книгу в нашем списке.

```jsx
// BookDetail.js
import React, { useState, useEffect } from 'react';
import { useParams, Link } from 'react-router-dom';

function BookDetail({ books }) {
  const { id } = useParams();
  const book = books.find(b => b.id === Number(id));
  const [bookDetails, setBookDetails] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  // Эффект для загрузки данных из API
  useEffect(() => {
    // Если книга не найдена, не делаем запрос
    if (!book) return;

    // Функция для выполнения запроса
    const fetchBookData = async () => {
      setLoading(true);
      setError(null); // Сбрасываем ошибку перед новым запросом
      try {
        // Формируем запрос к Google Books API
        // Ищем по названию и автору для большей точности
        const response = await fetch(
          `https://www.googleapis.com/books/v1/volumes?q=intitle:${encodeURIComponent(book.title)}+inauthor:${encodeURIComponent(book.author)}&maxResults=1`
        );

        if (!response.ok) {
          throw new Error('Не удалось загрузить данные о книге');
        }

        const data = await response.json();

        // Если API вернул результаты, берем первую найденную книгу
        if (data.items && data.items.length > 0) {
          setBookDetails(data.items[0]);
        } else {
          throw new Error('Книга не найдена в базе данных');
        }
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    };

    fetchBookData();
  }, [book]); // Эффект зависит от объекта book

  if (!book) {
    return (
      <div>
        <h2>Книга не найдена!</h2>
        <Link to="/">Вернуться на главную</Link>
      </div>
    );
  }

  return (
    <div>
      <Link to="/">← Назад к списку</Link>
      <h1>{book.title}</h1>
      <h2>Автор: {book.author}</h2>
      <p>Статус: <strong>{book.isRead ? 'Прочитана ✅' : 'Еще предстоит прочитать 📖'}</strong></p>

      {/* Состояние загрузки */}
      {loading && <p>Загружаем дополнительную информацию...</p>}

      {/* Сообщение об ошибке */}
      {error && <p style={{ color: 'red' }}>Ошибка: {error}</p>}

      {/* Отображение данных из API */}
      {bookDetails && !loading && (
        <div>
          {/* Показываем обложку, если она есть */}
          {bookDetails.volumeInfo.imageLinks?.thumbnail && (
            <img 
              src={bookDetails.volumeInfo.imageLinks.thumbnail} 
              alt={`Обложка книги ${book.title}`}
            />
          )}
          {/* Показываем описание, если оно есть */}
          {bookDetails.volumeInfo.description && (
            <div>
              <h3>Описание:</h3>
              <p>{bookDetails.volumeInfo.description}</p>
            </div>
          )}
        </div>
      )}
    </div>
  );
}

export default BookDetail;
```

**Что мы сделали:**

1.  **`useEffect` для побочного эффекта:** Запрос к API — это классический побочный эффект.
2.  **Асинхронный запрос:** Используем `async/await` для работы с Promise, который возвращает `fetch`.
3.  **Обработка состояний:** Мы управляем состояниями `loading` и `error`, чтобы давать обратную связь пользователю.
4.  **Безопасный доступ к данным:** Используем оператор опциональной цепочки `?.` на случай, если каких-то данных (например, обложки) в ответе API не окажется.

Теперь на детальной странице книги, если она найдена в Google Books, появятся обложка и описание! Наше приложение стало взаимодействовать с внешним миром.

Потрясающе! Наше приложение теперь получает живые данные из интернета. Однако интерфейс пока выглядит простовато. Давайте завершим наш проект, придав ему современный и профессиональный вид с помощью готовой библиотеки компонентов — UI-кита. В качестве примера будем использовать популярную библиотеку **Ant Design**.

***

#### **Этап 11: Внедрение UI-кита (Ant Design)**

UI-киты предоставляют нам готовые, стильные и доступные компоненты (кнопки, карточки, индикаторы загрузки), что избавляет нас от написания собственного CSS с нуля.

**Шаг 1: Импорт компонентов из Ant Design**

Предположим, что библиотека `antd` уже установлена в проект. Импортируем необходимые компоненты в наш `BookDetail.js`.

```jsx
// BookDetail.js
import React, { useState, useEffect } from 'react';
import { useParams, Link } from 'react-router-dom';
// Импортируем компоненты из Ant Design
import { Card, Spin, Alert, Tag } from 'antd';
// Импортируем иконку для спиннера
import { LoadingOutlined } from '@ant-design/icons';

function BookDetail({ books }) {
  const { id } = useParams();
  const book = books.find(b => b.id === Number(id));
  const [bookDetails, setBookDetails] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  // ... (useEffect для загрузки данных из API остается без изменений)

  if (!book) {
    // Используем компонент Alert для сообщения об ошибке
    return <Alert message="Книга не найдена!" description="Попробуйте вернуться на главную страницу." type="error" showIcon />;
  }

  return (
    <div style={{ padding: '24px' }}>
      <Link to="/">← Назад к списку</Link>

      {/* Заменяем простой div на компонент Card */}
      <Card
        title={book.title} // Заголовок карточки
        // Верхний правый угол карточки: цветной тег со статусом
        extra={<Tag color={book.isRead ? 'green' : 'blue'}>{book.isRead ? 'Прочитана' : 'К прочтению'}</Tag>}
        style={{ marginTop: 16 }}
      >
        {/* Основное содержимое карточки */}
        <p><strong>Автор:</strong> {book.author}</p>

        {/* Состояние загрузки: используем Spin из Ant Design */}
        {loading && (
          <div style={{ textAlign: 'center' }}>
            <Spin indicator={<LoadingOutlined style={{ fontSize: 24 }} spin />} />
            <p>Загружаем дополнительную информацию...</p>
          </div>
        )}

        {/* Сообщение об ошибке: используем Alert из Ant Design */}
        {error && <Alert message={error} type="warning" showIcon />}

        {/* Отображение данных из API */}
        {bookDetails && !loading && (
          <div>
            {/* Показываем обложку, если она есть */}
            {bookDetails.volumeInfo.imageLinks?.thumbnail && (
              <img
                src={bookDetails.volumeInfo.imageLinks.thumbnail.replace('http:', 'https:')} // Меняем протокол на https
                alt={`Обложка книги ${book.title}`}
                style={{ float: 'left', marginRight: '16px', marginBottom: '16px', maxHeight: '200px' }}
              />
            )}
            {/* Показываем описание, если оно есть */}
            {bookDetails.volumeInfo.description && (
              <div>
                <h3>Описание:</h3>
                {/* Описание может содержать HTML-теги, используем dangerouslySetInnerHTML с осторожностью */}
                <p dangerouslySetInnerHTML={{ __html: bookDetails.volumeInfo.description }} />
              </div>
            )}
          </div>
        )}
      </Card>
    </div>
  );
}

export default BookDetail;
```

**Что мы улучшили с помощью Ant Design:**

1.  **`Card`**: Заменили простой белый фон на стильную карточку с четко очерченными границами, заголовком и отступами. Это сразу придает информации структурированный и аккуратный вид.
2.  **`Tag`**: Вместо простого текста статус книги теперь отображается в виде цветного бейджа. Зеленый для "Прочитана", синий для "К прочтению". Это визуально более информативно.
3.  **`Spin`**: Индикатор загрузки стал гораздо элегантнее. Мы использовали встроенную иконку `LoadingOutlined`, которая автоматически анимируется.
4.  **`Alert`**: Сообщения об ошибках теперь выглядят как стандартные системные уведомления с иконкой, привлекающей внимание.

**Шаг 2: Применим Ant Design к другим компонентам (например, к списку книг)**

Давайте быстро улучшим и наш главный список, чтобы стиль был единым. Модифицируем `BookList.js`.

```jsx
// BookList.js
import React from 'react';
import { Link } from 'react-router-dom';
// Импортируем компоненты Ant Design
import { List, Button, Tag, Space } from 'antd';
import { CheckOutlined, ReadOutlined } from '@ant-design/icons';

function BookList({ books, setBooks }) {
  // Преобразуем наши книги в формат, подходящий для компонента List
  const data = books.map(book => ({
    ...book,
    // List будет использовать это свойство как ключ
    key: book.id,
  }));

  return (
    <div>
      <h2>Все книги</h2>
      {/* Заменяем <ul> на компонент List из Ant Design */}
      <List
        itemLayout="horizontal"
        dataSource={data}
        renderItem={(book) => (
          <List.Item
            actions={[
              // Кнопка теперь будет стилизованной
              <Button 
                type={book.isRead ? "default" : "primary"} 
                icon={book.isRead ? <ReadOutlined /> : <CheckOutlined />}
                onClick={() => {
                  const updatedBooks = books.map(b =>
                    b.id === book.id ? { ...b, isRead: !b.isRead } : b
                  );
                  setBooks(updatedBooks);
                }}
              >
                {book.isRead ? 'Не прочитана' : 'Прочитана'}
              </Button>
            ]}
          >
            <List.Item.Meta
              avatar={
                // Используем Tag для отображения статуса
                <Tag color={book.isRead ? 'green' : 'blue'}>
                  {book.isRead ? '✅' : '📖'}
                </Tag>
              }
              // Заголовок теперь является ссылкой на детальную страницу
              title={<Link to={`/book/${book.id}`}>{book.title}</Link>}
              description={`Автор: ${book.author}`}
            />
          </List.Item>
        )}
      />
    </div>
  );
}

export default BookList;
```

Теперь наше приложение выглядит современно и профессионально! Мы прошли огромный путь от простого статичного заголовка до полноценного, интерактивного, связанного с API и красивого SPA-приложения.

Безупречно! Мы с вами прошли огромный и очень содержательный путь. Давайте подведём итоги нашего занятия и наметим направления для дальнейшего самостоятельного развития.

***

### **Часть 6: Заключение и обзор (5 минут)**

**Этап 12: Итог и домашнее задание**

Итак, всего за полтора часа мы с вами создали полноценное, современное веб-приложение. Давайте вспомним весь наш путь от простого статичного заголовка до интерактивной одностраничной системы:

**Что мы реализовали вместе:**
1.  **Фундамент:** Создали первый React-компонент и познакомились с синтаксисом JSX.
2.  **Динамика:** Научили приложение хранить данные с помощью состояния `useState` и отображать их в динамическом списке.
3.  **Интерактивность:** Добавили возможность изменять состояние — отмечать книги как прочитанные.
4.  **Работа с формами:** Реализовали форму добавления новых книг, освоив концепцию контролируемых компонентов.
5.  **Сохранение данных:** Интегрировали `useEffect` и Local Storage для сохранения данных между сессиями.
6.  **Архитектура:** Провели рефакторинг, разделив приложение на мелкие, переиспользуемые компоненты (`BookList`, `AddBookForm`).
7.  **Маршрутизация:** Внедрили React Router, создав многостраничный интерфейс с навигацией.
8.  **Работа с API:** Научили приложение "разговаривать" с внешним миром, подгружая обложки и описания из Google Books API.
9.  **Пользовательский интерфейс:** Привели интерфейс к профессиональному виду с помощью библиотеки Ant Design.

Вы применили на практике все ключевые концепции React, которые необходимы для старта в фронтенд-разработке.

**Домашнее задание:**

Чтобы закрепить материал, попробуйте самостоятельно улучшить наше приложение, реализовав следующий функционал:

1.  **Удаление книг:** Добавьте кнопку удаления для каждой книги в списке. Вам потребуется создать функцию, которая будет фильтровать массив `books`, оставляя все книги, кроме той, `id` которой совпадает с удаляемой.

2.  **Фильтрация списка:** Добавьте три кнопки над списком: "Все", "Прочитанные", "Не прочитанные". Создайте состояние для активного фильтра и условно рендерьте отфильтрованный массив книг.

3.  **Валидация формы:** Улучшите форму добавления книги.
    *   Показывайте сообщения об ошибках под полями, если они пустые (после попытки отправки).
    *   Добавьте проверку на уникальность: не позвольте добавить книгу, если в списке уже есть книга с таким же названием и автором.
    *   Используйте компоненты `Form` и `Input` из Ant Design, которые имеют встроенную поддержку валидации.

Это задание позволит вам на практике поработать с условным рендерингом, обработкой массивов и улучшением пользовательского опыта.

Поздравляю вас с созданием первого полноценного React-приложения! Вы прекрасно справились. Желаю вам удачи в дальнейшем изучении увлекательного мира фронтенд-разработки. Если в ходе выполнения домашнего задания возникнут вопросы, не стесняйтесь их задавать.